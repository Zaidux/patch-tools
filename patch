#!/usr/bin/env python3
import os
import sys

# Add the patch-tool directory to Python path
tool_dir = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, tool_dir)

from patch_tool import main

if __name__ == "__main__":
    main()
